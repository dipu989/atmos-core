# Binaries
/bin/
/worker
*.exe
*.exe~
*.dll
*.so
*.dylib

# Build output
/tmp/
dist/

# Go build cache (not needed in repo)
*.test
*.out

# Environment — never commit real secrets
.env
.env.local
.env.*.local

# Local dev Docker Compose overrides — production EC2 must not have this
docker-compose.override.yml

# IDE
.idea/
.vscode/

# Claude Code local settings
.claude/
*.swp
*.swo
*.suo
.DS_Store

# Air (live reload)
air.toml
.air.toml
tmp/

# Local-only reference files — never committed
/tasks/
/atmos-api.postman_collection.json

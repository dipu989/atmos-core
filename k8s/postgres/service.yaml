apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: atmos
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
  # ClusterIP (default) — only reachable inside the cluster, not from outside
  type: ClusterIP
